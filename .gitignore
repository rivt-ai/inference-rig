# ─── IDEs & Editors ─────────────────────────────────────────
.idea/
.vscode/
*.iml
*.ipr
*.iws
*.swp
*.swo
*~
.*.sw[a-p]
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace
.history/
.fleet/

# ─── OS-generated files ─────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon
ehthumbs.db
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
.directory
.fuse_hidden*
.nfs*

# ─── AI / agent tooling ─────────────────────────────────────
.codex/
.claude/
.aider*
.cursor/
.continue/

# ─── Go ─────────────────────────────────────────────────────
# Binaries and build output
/inferencerig
.inferencerig
custom-golangci-lint
/dist/
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.o
*.a
# Coverage & profiling
coverage.txt
coverage.out
coverage.html
*.cover
*.prof
*.pprof
cpu.out
mem.out
# Coverage reports and provisioned E2E fixtures
artifacts/
# Go workspace / vendor / env
go.work
go.work.sum
vendor/
.env
.env.*
!.env.example

# ─── Generated protobuf / buf code ──────────────────────────
webui/web/src/lib/gen/

# ─── Web UI (Svelte / Vite / pnpm) ──────────────────────────
node_modules/
webui/node_modules/
webui/screenshots/
webui/.svelte-kit/
webui/.vite/
webui/build/
# The build output is not committed, but the directory must survive a fresh
# clone: `//go:embed all:dist` fails to compile against a missing directory.
# Excluding `webui/dist/` itself would make the negation below unreachable, so
# the contents are excluded instead.
webui/dist/*
!webui/dist/.gitkeep
webui/coverage/
webui/playwright-report/
webui/test-results/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ─── Local / scratch / secrets ──────────────────────────────
local/
tmp/
*.tmp
*.bak
*.log
*.pid
*.seed
*.local
config.local.yaml
config.yaml
!config.example.yaml
secrets/
*.pem
*.key
*.crt
