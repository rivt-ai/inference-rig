name: Live E2E

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: live-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    name: E2E (live)
    # Skip draft PRs. Stacked phase PRs stay draft until their phase is ready,
    # so live E2E only runs on PRs explicitly marked ready-for-review.
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      # Hardware tests skip unless the runner supplies the documented engine
      # and model environment variables.
      - name: Run live E2E
        run: make e2e-live
